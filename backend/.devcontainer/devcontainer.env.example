PYTHONPATH=/workspace
# Caché de uv compartida con el host
# El volumen está montado en docker-compose.local.yml desde ${LOCALAPPDATA}/uv/cache
# hacia /home/vscode/.cache/uv dentro del contenedor
UV_CACHE_DIR=/home/vscode/.cache/uv